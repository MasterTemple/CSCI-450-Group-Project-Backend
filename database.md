```
userId is email address

email_codes = [{ loginCode, emailAddress, time }]

user_logins = [{ authToken, emailAddress, time }]

song_list = [{
	songId,
	emailAddress,
	rawClipboardContents,
	title,
	author,
	breakIndexes,
	lines,
	slides,
	settings
}]

```
